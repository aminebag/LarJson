package com.aminebag.larjson.api;

/**
 * @author Amine Bagdouri
 *
 * A class implements this interface to indicate that it represents a JSON element.
 */
public interface LarJsonElement extends LarJsonPath, LarJsonWriteable, LarJsonCloneable {
}
