package com.aminebag.larjson.api;

import java.io.Closeable;

/**
 * @author Amine Bagdouri
 *
 * A class implements this interface to indicate that it represents a JSON root element.
 */
public interface LarJsonRootElement extends Closeable {
}
