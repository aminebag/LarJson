package com.aminebag.larjson.api;

/**
 * @author Amine Bagdouri
 *
 * A class implements this interface to indicate that it represents a JSON root element mapped to a model interface.
 */
public interface LarJsonRootTypedElement extends LarJsonTypedElement, LarJsonRootElement {
}
